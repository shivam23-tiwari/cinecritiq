const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // wait, we don't have service account, we can just use frontend SDK in a small script, or let's just query in the app code
