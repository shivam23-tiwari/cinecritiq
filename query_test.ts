import { initializeApp } from "firebase/app";
import { getFirestore, collection, getDocs, doc, getDoc } from "firebase/firestore";

// We can test this by running it in the client, but it's easier to just modify Profile.tsx.
